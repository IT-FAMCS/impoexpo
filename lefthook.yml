# EXAMPLE USAGE:
#
#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/
#
pre-commit:
  commands:
    check:
      run: cd packages/impoexpo-client && npm run lint -- --no-errors-on-unmatched
